#include "gtest/gtest.h"

#include "nigiri/loader/netex/service_journey_pattern.h"

using namespace nigiri;
using namespace nigiri::loader::netex;

TEST(netex, service_journey_pattern) {
  // TODO Take the ServiceFrame from the example and strip it down to the
  // minimum Check whether we correctly read in the servicejourneypatterns, also
  // check whether the references work
  ASSERT_TRUE(true);
}